export const metadata = {
  title: 'PHC Care Workspace | DoctaRx Nigeria',
  description: 'Programme-scoped assisted telehealth operations for primary health centres.',
};

export default function PhcLayout({ children }) {
  return children;
}
